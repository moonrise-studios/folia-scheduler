name: Deploy to Moonrise Nexus (Gradle)

on:
  push:
    branches:
      - '**'
  release:
    types:
      - published

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Configure Gradle / Nexus credentials
        run: |
          mkdir -p ~/.gradle
          cat << 'EOF' > ~/.gradle/gradle.properties
          nexusUsername=${{ secrets.NEXUS_USERNAME }}
          nexusPassword=${{ secrets.NEXUS_PASSWORD }}
          nexusSnapshotsUrl=https://repo.moonrise.gg/repository/maven-snapshots
          nexusReleasesUrl=https://repo.moonrise.gg/repository/maven-releases
          EOF

      - name: Modify version for release
        if: github.event_name == 'release'
        run: |
          sed -i "s/\\(var apiVersion = \\\"[^\\\"]*\\)-SNAPSHOT\\\"/\\1\\\"/" build.gradle.kts

      - name: Modify version for snapshot
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          sed -i "/var apiVersion = \\\".*-SNAPSHOT\\\"/! s/\\(var apiVersion = \\\"[^\\\"]*\\)\\\"/\\1-SNAPSHOT\\\"/" build.gradle.kts

      - name: Modify version for experimental
        if: github.event_name == 'push' && github.ref != 'refs/heads/main'
        run: |
          short_sha="${GITHUB_SHA::7}"
          sed -i "s/\\(var apiVersion = \\\"[^\\\"]*\\)-SNAPSHOT\\\"/\\1\\\"/" build.gradle.kts
          sed -i "s/\\(var apiVersion = \\\"[^\\\"]*\\)\\\"/\\1-${short_sha}\\\"/" build.gradle.kts

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build and deploy release
        if: github.event_name == 'release'
        run: ./gradlew clean publish -PisRelease=true

      - name: Build and deploy snapshot
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: ./gradlew clean publish -PisRelease=false

      - name: Build and deploy experimental
        if: github.event_name == 'push' && github.ref != 'refs/heads/main'
        run: ./gradlew clean publish -PisRelease=false -PnexusSnapshotsUrl=https://repo.moonrise.gg/repository/experimental
